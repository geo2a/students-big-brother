import { highlightCode } from 'students-big-brother-frontend-ember/helpers/highlight-code';
import { module, test } from 'qunit';

module('Unit | Helper | highlight code');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = highlightCode([42]);
  assert.ok(result);
});
